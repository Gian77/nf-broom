This is the `reads` repository where all your samples should be. Make sure all the sequence 
files for each sample (e.g. for example 1 genome) is in a separate directory to comply with
the pipeline requirements.
