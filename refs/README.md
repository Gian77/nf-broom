This is the `refs` repository where all your reference sequence data should be. For
example, organelle and reference genome sequences. Make sure all the sequence files, 
are in a named directory (e.g. for sorghum, I will have a refs/sorghum/ directory with all
the reference files) to comply with the pipeline requirements.

